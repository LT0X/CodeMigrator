# CM-<模块缩写>-<序号>-<简述>详细设计

> 使用说明：编码任务开工前复制本模板到 `codemigrator_design_doc/detailed_coding_design/<模块缩写>/CM-<模块缩写>-<序号>-<简述>详细设计.md` 填写。设计必须以 `architecture_module_design/` 对应 M-xx 文档为契约真相；公共契约只允许引用，禁止复制第二套定义（AGENTS.md §2.1）。若本设计与架构文档存在冲突，代码更新后必须回写架构文档并同步迭代记录（AGENTS.md §3.4）。

## 0. 元信息

- **日期**：YYYY-MM-DD
- **执行 Agent**：
- **关联任务编号**：T-Bxxx（主表编号）
- **所属模块/crate**：M-xx / `codemigrator-xxx`
- **依据的架构文档**：M-xx 具体章节名（如《M-09 沙箱与执行环境》"UDS 协议"一节）
- **关联交付物**：实施计划 `Implementation_plan_doc/<模块缩写>/…_实施计划.md`、迭代记录 `codemigrator_dev_progress/<模块缩写>/…_迭代记录.md`

## 1. 需求与边界

- 本任务做什么（一句话）：
- 不做什么（显式排除项）：
- 覆盖的验收条款：`V-Mxx-V4-nnn` 列表（逐条列出，禁止凭记忆转述，以架构文档原文为准）

## 2. 契约引用

| 引用事实 | Owner 文档 | 引用形式 |
| --- | --- | --- |
| 如：RunStatus / SliceKind / 错误码 | M-00 | `use codemigrator_core::…`，不重复定义 |

## 3. 详细设计

### 3.1 结构与职责

- 新增/修改的模块、trait、关键类型清单及各自职责边界（对齐 M-01 的 crate 禁止事项）

### 3.2 关键机制

- 核心流程（可用 mermaid 时序图/流程图）、状态转换、并发与事务边界、幂等键设计

### 3.3 数据与接口

- PostgreSQL 表/字段变更（对应 `migrations/`）、REST/SSE DTO（形状以 M-02 为准）、内部函数签名

### 3.4 错误处理

- 错误码复用清单（M-00 稳定错误码）；新增错误码须先在架构文档定案后引入

## 4. 测试设计

- 单测/集成测/恢复测/安全测分层清单；放置目录（`tests/contracts|recovery|security`）
- 每条验收条款对应的测试用例映射表：`V-Mxx-V4-nnn → 用例名`

## 5. 与架构文档的差异记录

- 无 / 有（逐条列出差异及原因；有差异时触发 AGENTS.md §3.4 回写义务）

## 6. 影响面

- 行为变化、兼容性、性能、需要同步更新的其他 M-xx 文档或目录内文档

## 7. 附录（可选）

- 权衡备选方案、参考链接、原型代码片段
