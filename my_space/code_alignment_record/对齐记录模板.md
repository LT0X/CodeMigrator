# CM-<MODULE>-001 对齐记录

> 用途：本文件是任务 `<任务ID>`（模块 \<M-xx 中文名>）编码前与用户对齐的契约记录，goal 模式下实现 agent 以本文件为自主开发依据。
> 维护规则：问答记录与变更记录 append-only（只增不改）；再对齐须经用户确认并走主任务表 §8 变更流程；禁止写入敏感凭证。

## 0. 元信息

| 字段     | 内容                                                                   |
| ------ | -------------------------------------------------------------------- |
| 任务ID   | `<CM-XXX-001，与主任务表 §7.3 一致>`                                         |
| 模块编号   | `<M-xx>`                                                             |
| 对应设计文档 | `my_space/codemigrator_design_doc/architecture_module_design/<文件名>`  |
| 主任务表   | `my_space/codemigrator_dev_progress/CodeMigrator开发任务规划与进度跟踪.md` §7.3 |
| 对齐轮次   | `<第 N 轮，如 Wave 0+1 轮>`                                               |
| 对齐日期   | `YYYY-MM-DD`                                                         |
| 对齐参与者  | `用户 + <agent 标识>`                                                    |
| 对齐状态   | `已对齐 / 待定项押后（注明）`                                                    |

## 1. 任务理解

### 1.1 范围（做什么）

`<依据设计文档与主任务表，列出本任务交付的能力>`

### 1.2 边界（不做什么）

`<设计文档明确排除项 + 与相邻模块的边界划分>`

### 1.3 产出物

`<crate/目录/文件/测试等可验证交付物清单>`

## 2. 关键实现决策与确认结论

> 仅记录经用户确认的结论；每条注明依据。agent 不得替用户决定。

| #    | 决策点             | 可选项             | 用户确认结论      | 依据                       |
| ---- | --------------- | --------------- | ----------- | ------------------------ |
| D-01 | `<实现层面需要裁决的问题>` | `<A / B / ...>` | `<用户选择的方案>` | `<设计文档章节 / 验收条款 / 对齐问答>` |

## 3. 接口与依赖契约摘要

### 3.1 上游输入

`<本任务消费的类型/服务及来源模块（M-xx）>`

### 3.2 下游消费

`<本任务提供给哪些模块（M-xx）及内容>`

### 3.3 跨模块接口边界

`<与相邻模块的关键边界约定（谁拥有什么、禁止什么）>`

## 4. 验收条款映射

| 条款             | 内容摘要    | 验证方式          |
| -------------- | ------- | ------------- |
| `V-Mxx-V4-001` | `<一句话>` | `<测试类型/命令形态>` |

## 5. 风险与注意点

* `<设计文档标注的陷阱、跨模块交叉验收条款、易错边界>`

## 6. 对齐问答记录

> append-only：每轮对齐的问题与用户结论。

| #    | 问题         | 用户结论     |
| ---- | ---------- | -------- |
| Q-01 | `<问题原文要点>` | `<结论要点>` |

## 7. 变更记录

| 日期         | 变更   | 说明     |
| ---------- | ---- | ------ |
| YYYY-MM-DD | 建立记录 | `<依据>` |

