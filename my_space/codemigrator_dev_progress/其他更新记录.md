# 其他更新记录

> 用途：记录与 CodeMigrator 模块代码无直接关系的更新（基础设施配置、虚拟机环境变更、隧道端口调整、CI/构建脚本、目录结构调整等）。模块代码相关的变更走各模块迭代记录，不要写在这里。
> 维护规则：append-only，最新条目在最上方。

| 日期         | Agent    | 类别            | 变更摘要                                                                                                                                                                                                                                                                                                                                                                                                       | 影响/后续行动                                                                                                                                                                          |
| ---------- | -------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-28 | ox-alpha | 环境配置(V5 语言重配) | 设计文档从 Rust V4 迁移为 Python V5（2026-08-27，见迭代记录），对 dev\@Ubuntu-24.04 做环境适配：新增 uv 0.12.7+uvx（隔离安装 \~/env/uv，PyPI 源=清华 TUNA，见 \~/.config/uv/uv.toml，PATH 由安装器 source \~/env/uv/env）；系统 python3.12.3 由 uv 自动发现；Rust 经用户确认保留不动（V5 后端弃用，Node 因 Web 仍 React+TS 保留）；.env 生效段改回 dev\@24.04 并新增 \[V5 技术栈基线]，曾误标生效的 xtc\@22.04 内容移入文末存档段                                                                                | 后端依赖（FastAPI/SQLAlchemy 2.0+asyncpg/Pydantic v2/py-tree-sitter/structlog+OTel/import-linter）为项目级依赖，待脚手架 pyproject.toml 建立后经 uv 装入项目 .venv；沙箱改 app 直接调 bwrap，bwrap/Docker/PG 无需改动 |
| 2026-08-26 | ox-alpha | 环境配置(修正)      | ①修正磁盘误报：此前记录"954G 可用"是动态 VHDX 虚拟上限；实测 VHDX 位于 D:\wsl\Ubuntu-24.04，宿主 D 盘总 267G/可用 236G，用户约束 WSL 配额至多约 100G（已回填 .env）②apt 源切换国内镜像：/etc/apt/sources.list.d/ubuntu.sources 的 archive/security.ubuntu.com → mirrors.aliyun.com/ubuntu（原文件备份 .bak），apt update 验证通过                                                                                                                                              | 大型依赖安装前按 100G 配额预算评估磁盘占用                                                                                                                                                         |
| 2026-08-26 | ox-alpha | 环境配置          | WSL2(Ubuntu 24.04) 完成基础环境搭建：Docker CE 29.7.2 + compose v5.5.0（apt 走阿里云源；registry-mirrors 按 temp/supply/docker\_mirror.md 去重配置 37 条，hello-world 验证通过）；bubblewrap 0.9.0（--unshare-all 冒烟通过）；Rust 1.98.0 stable（rustup/rsproxy，装 \~/env）；Node v24.19.0 + pnpm 11.24.0（npmmirror，装 \~/env）；gcc/psql 客户端等 apt 件；工具链 PATH 接线 \~/.profile+.bashrc 托管块；AGENTS.md 已先行适配为 WSL 单环境规范，my\_space/.env 重写为 WSL 拓扑并回填全部记录 | M-09 沙箱四项前提（kernel/cgroup v2/bubblewrap/userns）全部满足；本机沙箱执行位=1（7.8G/4C）；待批次1建立 compose.yaml 后部署 PostgreSQL 并回填 .env                                                               |
| 2026-08-24 | ox-alpha | 目录结构          | 设计文档目录扁平化（消除 codemigrator\_design\_doc 双层嵌套）；建立 my\_space 工作空间骨架（dev\_progress/Implementation\_plan\_doc/temp/pr\_md 等）；AGENTS.md 改版为 CodeMigrator 开发规范                                                                                                                                                                                                                                                    | 无                                                                                                                                                                                |

